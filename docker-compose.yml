version: "3.8"
services:
  sourcingdb:
    image: mongo
  product.api:
    image: product_api_image
    build:
     context: .
     dockerfile: ESourcing.Products/Dockerfile
volumes:
  sourcingdb_volume: